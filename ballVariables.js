const radius = 15;
const diameter = 2 * radius;

let xBallPos = _width / 2;
let yBallPos = _height / 2;
let xBallSpeed = 9;
let yBallSpeed = 7;

const xBallLimit = _width - radius;
const yBallLimit = _height - radius;
